{
  "name": "stellar-cli-agent-docs",
  "private": true,
  "scripts": {
    "prebuild": "mkdir -p static && cp llms-agent-cli.txt llms-agent-cli-full.txt static/",
    "start": "npm run prebuild && docusaurus start",
    "build": "docusaurus build",
    "serve": "docusaurus serve",
    "clear": "docusaurus clear",
    "llms": "node scripts/build-llms.mjs"
  },
  "dependencies": {
    "@docusaurus/core": "^3.7.0",
    "@docusaurus/preset-classic": "^3.7.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
